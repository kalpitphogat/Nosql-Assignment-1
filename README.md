# NoSQL Systems (DAS 838) — Assignment 1

Team submission. Due 15 Sept, 8:00 AM.

- `problem1_tpch/` — Scaling study using the TPC-H benchmark (PostgreSQL)
- `problem2_integration/` — Incomplete data integration + JSON generation
- `problem3_unix_pipeline/` — Unix data-processing pipeline

Each subdirectory has its own `README.md` with the approach, how to
reproduce it, and results/analysis. Datasets are not included in the
final submission zip per the assignment instructions (customer
master/incoming CSVs are the exception — they're the supplied assignment
inputs, not generated benchmark data, and are small enough to include).
